# 1.8 Create Quiz System

## Tujuan
- Komponen quiz MCQ, skor & feedback, terhubung ke progres.

## Implementasi
- Schema: `quizzes` di `convex/schema.ts`.
- Convex: `quizzes.listByLesson`, `quizzes.create`, `quizzes.submit` (skoring + feedback; jika skor ≥ 60 → catat `progress`).
- UI: Quiz di halaman pelajaran `src/app/(app)/lms/[course]/[module]/[lesson]/page.tsx` (form radio per soal, server action submit).

## Verifikasi
- `type-check`, `lint`, `build` OK.
- Route lesson menampilkan blok Quiz jika data tersedia.

## Bukti
- Build output lesson route.
- `submit` mengembalikan skor dan feedback, dan menandai selesai jika lulus.

## Checklist
- [x] MCQ quiz.
- [x] Skoring & feedback.
- [x] Terhubung ke progres pelajaran.
