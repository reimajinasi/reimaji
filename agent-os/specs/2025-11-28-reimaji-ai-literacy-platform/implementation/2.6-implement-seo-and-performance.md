# 2.6 Implement SEO & Performance

## Ringkasan
- Meta tags, Open Graph, Twitter cards, structured data (JSON-LD), robots, dan sitemap.
- Perbaikan kecepatan dengan metadata routes dan responsif yang sudah diterapkan.

## Implementasi
- Update `src/app/layout.tsx` metadata (title, description, keywords, robots, openGraph, twitter).
- Tambah `src/app/robots.ts` dan `src/app/sitemap.ts` (metadata routes) + `public/robots.txt` fallback.
- Tambah JSON-LD di `src/app/page.tsx` (WebSite), `news/[slug]` (NewsArticle), `research/[slug]` (Article).

## Acceptance
- Sitemap & robots tersedia, meta dan structured data terpasang, halaman cepat dimuat.

## Verifikasi
- `npm run type-check`, `npm run lint`, `npm run build` hijau.
- Uji manual: akses `/sitemap.xml` dan `robots.txt` menampilkan konten benar.

## Risiko/Follow-up
- Tambah image og: dan canonical per halaman saat konten final tersedia.
