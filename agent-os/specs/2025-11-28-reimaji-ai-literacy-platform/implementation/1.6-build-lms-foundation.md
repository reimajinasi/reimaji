# 1.6 Build LMS Foundation

## Tujuan
- Struktur kursus → modul → pelajaran.
- Halaman overview kursus dengan navigasi dan progres.

## Implementasi
- Schema: tambah `progress`, `quizzes` di `convex/schema.ts`.
- Convex: `courses.ts`, `modules.ts`, `lessons.ts`, `progress.ts` (list/get/create/update/remove, progres user).
- UI:
  - Katalog kursus: `src/app/(app)/lms/page.tsx` (list published).
  - Overview kursus: `src/app/(app)/lms/[course]/page.tsx` (modul + daftar pelajaran, progres %).

## Verifikasi
- `type-check` OK, `lint` OK, `build` OK.
- Routes: `/lms`, `/lms/[course]` terdeteksi.

## Bukti
- Build output route LMS.
- Progres dihitung dari tabel `progress` terhadap jumlah pelajaran published.

## Checklist
- [x] Struktur kursus → modul → pelajaran.
- [x] Overview + navigasi.
- [x] Progres per kursus.
