# 0.7 Design System Foundations – Implementasi

## Aksi
- Konfigurasi tokens Tailwind v4 di `src/app/globals.css` untuk warna, radius, ring, dan tipografi.
- Tambah utilitas `cn` di `src/lib/utils.ts` dengan `clsx` + `tailwind-merge`.
- Tambah komponen UI dasar di `src/components/ui/`: `button.tsx`, `card.tsx`, `input.tsx`, `separator.tsx`, `skeleton.tsx`, `breadcrumb.tsx`.
- Tambah komponen layout reusable di `src/components/layout/`: `page-header.tsx`, `content-card.tsx`, `loading-state.tsx`, `container.tsx`, `section.tsx`.
- Instal dependensi: `clsx`, `tailwind-merge`.
- Terapkan konsistensi spacing: bungkus `MainNav` dengan `Container` di `src/app/layout.tsx`; gunakan `Container` di layout `(public)/(app)/admin`.
- Bersihkan Home dari template Next.js: gunakan `PageHeader`, `Section`, `ContentCard`, dan `Button` minimal.

## Checklist
- [x] 0.7.1 Configure shadcn/ui-like components
- [x] 0.7.2 Define color palette & typography tokens (Tailwind v4)
- [x] 0.7.3 Create reusable layout components

## Bukti
- Type-check: `npm run type-check` → OK
- Lint: `npm run lint` → OK
- Build: `npm run build` → OK
- Routes ter-generate normal, tanpa error.
  - Halaman yang memakai komponen baru: `news`, `research`, `dashboard`, `lms`, `admin`, dan `home`.

## Catatan Teknis
- Alias path `@/*` digunakan, impor diperbaiki ke `@/lib/*` dan `@/components/*`.
- Menghindari konfigurasi `tailwind.config.ts` karena Tailwind v4 memakai `@theme` CSS.
- Komponen dibuat minimal dan konsisten dengan tokens, siap dikembangkan lanjut.
