# 0.6 – Create Folder Structure and Routing Skeleton

## Tujuan
- Menyusun struktur App Router: `(public)`, `(app)` (dashboard, lms), `admin`.
- Menambah layout dasar dan navigasi.
- Verifikasi halaman render tanpa error dan navigasi berfungsi.

## Tindakan
1. Route groups & pages:
   - `(public)/news/page.tsx`, `(public)/research/page.tsx`.
   - `(app)/dashboard/page.tsx`, `(app)/lms/page.tsx`.
   - `admin/page.tsx`.
2. Layouts:
   - `(public)/layout.tsx`, `(app)/layout.tsx`, `admin/layout.tsx` dengan wrapper container.
   - `src/app/layout.tsx` ditambah `MainNav` di bawah `AuthHeader`.
3. Navigasi:
   - `components/layout/main-nav.tsx` (Home, News, Research).
   - `components/layout/app-nav.tsx` (Dashboard, LMS).
4. Perbaikan build:
   - Hapus duplikasi rute `/dashboard` & `/lms` top-level.
   - Bersihkan cache `.next` dan rebuild.
   - Sesuaikan `tsconfig.json` untuk stabilitas type-check.

## Checklist
- [x] Struktur folder `(public)/(app)/admin` dibuat.
- [x] Layout dasar untuk tiap area dibuat.
- [x] Komponen navigasi dibuat dan dipasang (MainNav pada root).
- [x] Halaman dashboard & lms tersedia pada `(app)`.
- [x] Halaman admin tersedia.
- [x] Type-check, lint, build lulus.

## Bukti
- Type-check: `tsc --noEmit` lulus.
- Lint: lulus dengan 4 warnings dari file auto-generated Convex.
- Build: berhasil; daftar route:
  - `/`, `/admin`, `/dashboard`, `/lms`, `/news`, `/protected`, `/research`, `/sign-in`, `/sign-up`, `/sign-out`.
- Proxy aktif sesuai `src/proxy.ts`.

## Catatan
- Navigasi publik terlihat di seluruh halaman via root layout.
- Rute `(app)` dan `admin` terproteksi oleh proxy; akses memerlukan login.
