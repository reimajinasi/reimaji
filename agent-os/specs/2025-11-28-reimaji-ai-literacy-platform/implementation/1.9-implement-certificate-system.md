# 1.9 Implement Certificate System

## Tujuan
- Sertifikat dapat diunduh (PDF via print) setelah menyelesaikan kursus.

## Implementasi
- Halaman sertifikat: `src/app/(app)/lms/[course]/certificate/page.tsx` dengan tombol "Cetak PDF" (`window.print()`) dan layout siap print.
- Validasi progres lengkap dapat ditambahkan di server (lanjutan) untuk gating akses.

## Verifikasi
- `type-check`, `lint`, `build` OK.
- Route `/lms/[course]/certificate` terdeteksi.

## Bukti
- Build output route certificate.
- Halaman siap dicetak sebagai PDF.

## Checklist
- [x] Halaman sertifikat siap cetak.
- [ ] Gating akses berdasarkan progres (lanjutan).
