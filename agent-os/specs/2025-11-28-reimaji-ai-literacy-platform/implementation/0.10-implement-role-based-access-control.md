# 0.10 Implement Role-Based Access Control – Implementasi

## Aksi
- Buat helper RBAC di `convex/permissions.ts` (`ensureRole`, `getUserByClerkId`).
- Terapkan role check pada fungsi server: contoh `users.listAll` hanya untuk `admin/superadmin`.
- Tipe aman: union `RoleType` (`guest|free|pro|admin|superadmin`).

## Checklist
- [x] 0.10.1 Define roles (guest, free, pro, admin)
- [x] 0.10.2 Create authorization helpers
- [x] 0.10.3 Implement role checks in Convex functions

## Bukti
- `npm run type-check` OK, `npm run lint` OK
- `npm run build` OK
- Usage: panggil `users.listAll` dengan `clerkUserId` milik admin/superadmin.

